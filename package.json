{
  "name": "attractor-cooperation",
  "version": "0.2.1-draft",
  "private": true,
  "type": "module",
  "description": "Draft profile for verifiable transmission between AI agents: spec, reference checker, conformance cases.",
  "license": "MIT AND CC-BY-4.0",
  "scripts": {
    "test": "node conformance/run.mjs",
    "cross-check": "python conformance/cross-check.py",
    "schema": "node conformance/schema-check.mjs"
  },
  "devDependencies": {
    "ajv": "^8.20.0"
  }
}
